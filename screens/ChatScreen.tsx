import React, { useRef, useLayoutEffect, useEffect, useState } from 'react';
import { View, Text, Image, StyleSheet, SafeAreaView, TouchableOpacity, Alert } from 'react-native';

const ChatScreen = () => {
  return (
    <View>
      <Text>ChatScreenn</Text>
    </View>
  );
};

export default ChatScreen;
